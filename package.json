{
    "scripts": {
        "dev": "npm run build -- --sourcemap=inline --watch",
        "build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/jupyter_host_filebrowser/static --loader:.svg=text",
        "fix:prettier": "npm run lint:prettier -- --write",
        "lint:prettier": "prettier --config package.json --check",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@jupyterlab/coreutils": "^6.5.3",
        "@jupyterlab/ui-components": "^4.5.3"
    },
    "devDependencies": {
        "@anywidget/types": "^0.2.0",
        "esbuild": "^0.25.4",
        "prettier": "^3.7.4",
        "typescript": "^5.8.3"
    },
    "prettier": {
        "printWidth": 88,
        "tabWidth": 4
    }
}
